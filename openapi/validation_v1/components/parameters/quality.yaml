name: quality
required: false
in: query
description: >-
  Optional flag specifying the minimum signature quality required (SES, AES,
  QES).
schema:
  type: string
  enum:
    - SES
    - AES
    - QES
