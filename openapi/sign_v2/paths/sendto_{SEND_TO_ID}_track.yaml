get:
  tags:
    - SendTo
  summary: Track Send-To
  description: >-
    To call the `track` endpoint, you must include the value of the
    `access_code` field—received when creating the `Send-To` object—in the
    request header `X-Accesscode`.
  operationId: getSendToTrack
  security:
    - bearer: []
  parameters:
    - $ref: '../components/parameters/XAccesscodeHeader.yaml'
    - $ref: '../components/parameters/SendToIdPath.yaml'
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/TrackSendToResponse.yaml'
          examples:
            unclaimed:
              $ref: '../components/examples/TrackSendToResponse.yaml#/SendToUnclaimed'
            claimed:
              $ref: '../components/examples/TrackSendToResponse.yaml#/SendToClaimed'
            signed:
              $ref: '../components/examples/TrackSendToResponse.yaml#/SendToSigned'
    400:
      $ref: ../components/responses/BadRequest.yaml
      # TODO
    401:
      $ref: ../components/responses/Unauthorized.yaml
    404:
      $ref: ../components/responses/NotFound.yaml
    429:
      $ref: ../components/responses/TooManyRequests.yaml
    500:
      $ref: ../components/responses/ServerError.yaml
    503:
      $ref: ../components/responses/ServiceUnavailable.yaml
